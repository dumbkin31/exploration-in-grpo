# The paper's validation decoding (Liang et al. 2026, Table 3: top_p 0.8, top_k 20; temperature
# not stated in the extraction we have, assumed 1.0). Use for comparability with the paper's
# tables; `default.yaml` for Qwen's recommended non-thinking settings.
defaults:
  - default
  - _self_
sampling:
  temperature: 1.0
  top_p: 0.8
  top_k: 20
