# Minimal environment for `make prefetch` on the CentOS 7 login node (glibc 2.17):
# pure-python downloads only. No torch, no pyarrow (their wheels need glibc >= 2.28).
huggingface_hub[hf_transfer]>=0.30
