# CPU-only development / test environment (Mac laptop or the Ada login node).
# This does NOT include verl or vLLM; tests that need them are skip-guarded.
# torch is pinned to the same version as the cluster so API drift is caught early.
torch==2.11.0
numpy>=2.0.0
pytest>=8.0
pytest-timeout>=2.3
math-verify==0.9.0
latex2sympy2_extended==1.11.0
pyyaml>=6.0
omegaconf>=2.3
hydra-core>=1.3
pandas>=2.2
pyarrow>=19.0.0
datasets>=3.0
ruff>=0.6
# For the tokenizer-only chat-template preflight check on a laptop (scripts/check_env.py --only-config):
transformers==5.5.3
huggingface_hub>=0.30
