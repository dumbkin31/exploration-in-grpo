# Arm 2: Mixed-CUTS on MATH. 8 standard + 8 CUTS rollouts per prompt (paper Table 3).
hydra:
  searchpath:
    - pkg://verl.trainer.config
defaults:
  - base_grpo
  - _self_
mixed_cuts:
  enabled: true
  group_size: 16
  n_std: 8
  n_cuts: 8
